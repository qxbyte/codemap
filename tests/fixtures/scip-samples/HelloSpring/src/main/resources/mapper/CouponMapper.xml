<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.hellospring.mapper.CouponMapper">
  <select id="selectByUser" resultType="java.math.BigDecimal">
    SELECT amount FROM sf_coupon WHERE user_id = #{userId}
  </select>
</mapper>
